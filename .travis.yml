dist: trusty
language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
      sudo: required
      
addons:
  apt:
    packages:
      - cmake
      - libopencv-dev
       
script:
  - sudo mkdir build
  - cd build
  - cmake .. 
  - make -j4
